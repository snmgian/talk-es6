h2 Promises

p Asynchronous handling:

== hl_code :javascript
  | getHtml('/slides/1').then(function(html) {
      var $content = document.querySelector('#content');
      $content.innerHTML = html;

    }, function(error) {
      console.log("Failed!", error);
    });
